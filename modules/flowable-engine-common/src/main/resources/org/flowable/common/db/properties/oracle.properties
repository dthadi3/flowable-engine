limitBefore=select * from ( select a.*, ROWNUM rnum from (
limitBeforeNew=select RES.* from ( select RES.*, rownum as rnum from (
limitAfter= ) a where ROWNUM < #{lastRow}) where rnum  >= #{firstRow}
limitAfterNew= ) RES where ROWNUM < #{lastRow} ) RES where rnum >= #{firstRow}
boolValue=1
